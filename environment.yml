name: session-env
channels:
  - conda-forge
  - defaults
dependencies:
  - python=3.12
  - pip

  # Python Toolchain Packages (Managed via pip to match your dev flags)
  - pip:
    - -e .[dev]   # Installs sesh in editable mode with the numpy/torch/mlflow
                  # extras and dev tools (ruff, mypy, pytest)
